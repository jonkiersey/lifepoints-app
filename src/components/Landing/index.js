import React from 'react';
 
const Landing = () => (
  <div>
    <h1>where the points <i>do</i> matter</h1>
    <p>After creating an account an signing in, head over to Home to get started</p>
  </div>
);
 
export default Landing;